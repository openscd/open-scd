import { expect } from '@open-wc/testing';

import { FormField, Value, FormGroup } from '../src/index.js';


describe('FormGroup', () => {
  it('should do stuff', () => {
    const t = true;
    expect(t).to.be.false;
  });
});
